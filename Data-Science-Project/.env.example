# Copy this file to .env and fill in your values.
# .env is gitignored — never commit secrets.

# Path overrides (optional — defaults to project-relative paths)
# RAW_DATA_DIR=data/raw
# PROCESSED_DATA_DIR=data/processed
# MODELS_DIR=models
# REPORTS_DIR=reports
